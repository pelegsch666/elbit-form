export default function Tools() {
  const trashEmoji = "🗑️";
  const editEmoji = "✏️";

  return (
    <>
      <button onClick={() => {}}>{trashEmoji}</button>
      <button onClick={() => {}}>{editEmoji}</button>
    </>
  );
}
